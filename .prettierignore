# Kept verbatim — the original spec.
DESIGN.md

# Golden test fixtures are generated; byte-stability matters more than style.
**/testdata/
dist/
bin/

AGENTS.md
CLAUDE.md
.claude/

# Generated from the cobra command tree (mise run docs); same reasoning as the
# fixtures above — a reformat would surface as drift on the next regeneration.
# docs/cli.md, the hand-written page that navs to them, is deliberately not
# covered.
docs/cli/
completions/

# goreleaser config carries Go templates prettier mangles
.goreleaser.yaml

# release-please writes both; its release PRs must not trip the format gate
CHANGELOG.md
.release-please-manifest.json

# Shared toolchain submodule (linted in its own repo)
.mise/

# helm templates are Go templates, not YAML prettier can parse
charts/patchy/templates/
charts/patchy-config/templates/

# Docs site build output (zensical)
/site/


# Status page SPA sub-project (its own toolchain; node_modules/dist live here)
internal/web/ui/
