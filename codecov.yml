# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# Codecov configuration. Generated code is excluded so patch coverage
# reflects only hand-written lines — deepcopy funcs are regenerated by
# `mise run codegen` and never edited by hand.
ignore:
  - "**/zz_generated*.go"
